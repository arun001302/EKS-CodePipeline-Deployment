apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "java-ci-cd-app.fullname" . }}
  labels:
    app: {{ include "java-ci-cd-app.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "java-ci-cd-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "java-ci-cd-app.name" . }}
    spec:
      containers:
        - name: java-ci-cd-app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8080
